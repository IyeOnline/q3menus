#pragma once
#include <iostream>
#include <string>
#include <algorithm>

namespace q3menus
{
	const size_t debuglevel = 5;
	
	void debug(const size_t treshhold,std::string name, const std::string& text);
}

// kate: space-indent off; replace-tabs off
